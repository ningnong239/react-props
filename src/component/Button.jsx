export default function Button({ buttonText }) {
    return (
        <button style={{ padding: '10px 20px', margin: '10px', fontSize: '16px' }}>
          {buttonText}
        </button>
      );
    }